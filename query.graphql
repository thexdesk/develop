query {

    codex {

        projects {
            key
        }

        phpdoc(projectKey:"blade-extensions", revisionKey:"master"){
            files {
                name
                hash
                type
            }

        }
    }
}
